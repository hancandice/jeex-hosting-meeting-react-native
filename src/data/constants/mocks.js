const categories = [
  {
    id: 1,
    name: "Plants",
    tags: ["products", "inspirations", "shop"],
  },
];

const products = [];

const explore = [
  // images
];

const profile = {};

export { categories, products, profile, explore };
